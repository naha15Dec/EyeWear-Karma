@model MatKinh.ViewModel.Register
@{
    ViewBag.Title = "Đăng ký";
}

<section class="auth-page">
    <section class="optical-breadcrumb">
        <div class="container">
            <div class="optical-breadcrumb__inner">
                <span class="optical-breadcrumb__eyebrow">Karma Eyewear Account</span>
                <h1>Tạo tài khoản mới</h1>
                <nav>
                    <a href="@Url.Action("Index","Home")">Trang chủ</a>
                    <span>/</span>
                    <span>Đăng ký</span>
                </nav>
            </div>
        </div>
    </section>

    <section class="auth-section">
        <div class="container">
            <div class="auth-shell">
                <div class="row no-gutters">
                    <div class="col-lg-5">
                        <div class="auth-showcase auth-showcase--register">
                            <div class="auth-showcase__image">
                                <img src="@Url.Content("~/Asset/img/login.jpg")" alt="Đăng ký tài khoản" />
                            </div>

                            <div class="auth-showcase__overlay"></div>

                            <div class="auth-showcase__content">
                                <span class="auth-showcase__eyebrow">Already a member?</span>
                                <h2>Đã có tài khoản?</h2>
                                <p>
                                    Đăng nhập để tiếp tục mua sắm, theo dõi đơn hàng và nhận những gợi ý
                                    mắt kính phù hợp với phong cách của bạn.
                                </p>
                                <a class="optical-btn optical-btn--light auth-showcase__btn"
                                   href="@Url.Action("LoginAccount","Account")">
                                    Đăng nhập ngay
                                </a>
                            </div>
                        </div>
                    </div>

                    <div class="col-lg-7">
                        <div class="auth-form-wrap auth-form-wrap--register">
                            <div class="auth-form-head">
                                <span class="auth-form-head__eyebrow">Create account</span>
                                <h3>Đăng ký</h3>
                                <p>Tạo tài khoản để lưu thông tin, mua sắm tiện lợi và nhận hỗ trợ tốt hơn.</p>
                            </div>

                            @using (Html.BeginForm("RegisterAccount", "Account", FormMethod.Post, new { @class = "auth-form auth-form--register", id = "registerForm", autocomplete = "off" }))
                            {
                                @Html.AntiForgeryToken()

                                <div class="auth-summary-box">
                                    @Html.ValidationSummary(true, "", new { @class = "auth-validation-summary" })
                                    <div id="ajaxMessage" class="auth-ajax-message"></div>
                                </div>

                                <div class="auth-grid-2">
                                    <div class="auth-form-group auth-grid-full">
                                        <label class="auth-label">Tên đăng nhập</label>
                                        @Html.TextBoxFor(m => m.Username, new
                                        {
                                            @class = "auth-input",
                                            placeholder = "Nhập tên đăng nhập"
                                        })
                                        @Html.ValidationMessageFor(m => m.Username, "", new { @class = "text-danger small d-block mt-2" })
                                    </div>

                                    <div class="auth-form-group">
                                        <label class="auth-label">Mật khẩu</label>
                                        @Html.PasswordFor(m => m.Password, new
                                        {
                                            @class = "auth-input",
                                            placeholder = "Nhập mật khẩu"
                                        })
                                        @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger small d-block mt-2" })
                                    </div>

                                    <div class="auth-form-group">
                                        <label class="auth-label">Nhập lại mật khẩu</label>
                                        @Html.PasswordFor(m => m.ConfirmPassword, new
                                        {
                                            @class = "auth-input",
                                            placeholder = "Nhập lại mật khẩu"
                                        })
                                        @Html.ValidationMessageFor(m => m.ConfirmPassword, "", new { @class = "text-danger small d-block mt-2" })
                                    </div>

                                    <div class="auth-form-group">
                                        <label class="auth-label">Số điện thoại</label>
                                        @Html.TextBoxFor(m => m.Mobile, new
                                        {
                                            @class = "auth-input",
                                            placeholder = "Nhập số điện thoại",
                                            type = "tel",
                                            autocomplete = "tel"
                                        })
                                        @Html.ValidationMessageFor(m => m.Mobile, "", new { @class = "text-danger small d-block mt-2" })
                                    </div>

                                    <div class="auth-form-group">
                                        <label class="auth-label">Email</label>
                                        @Html.TextBoxFor(m => m.Email, new
                                         {
                                             @class = "auth-input",
                                             placeholder = "Nhập email",
                                             type = "email",
                                             autocomplete = "email"
                                         })
                                        @Html.ValidationMessageFor(m => m.Email, "", new { @class = "text-danger small d-block mt-2" })
                                    </div>

                                    <div class="auth-form-group">
                                        <label class="auth-label">Họ</label>
                                        @Html.TextBoxFor(m => m.LastName, new
                                        {
                                            @class = "auth-input",
                                            placeholder = "Nhập họ"
                                        })
                                        @Html.ValidationMessageFor(m => m.LastName, "", new { @class = "text-danger small d-block mt-2" })
                                    </div>

                                    <div class="auth-form-group">
                                        <label class="auth-label">Tên</label>
                                        @Html.TextBoxFor(m => m.FirstName, new
                                        {
                                            @class = "auth-input",
                                            placeholder = "Nhập tên"
                                        })
                                        @Html.ValidationMessageFor(m => m.FirstName, "", new { @class = "text-danger small d-block mt-2" })
                                    </div>

                                    <div class="auth-form-group">
                                        <label class="auth-label">Ngày sinh</label>
                                        @Html.TextBoxFor(m => m.DateOfBirth, new
                                        {
                                            @class = "auth-input",
                                            type = "date"
                                        })
                                        @Html.ValidationMessageFor(m => m.DateOfBirth, "", new { @class = "text-danger small d-block mt-2" })
                                    </div>

                                    <div class="auth-form-group">
                                        <label class="auth-label">Giới tính</label>
                                        @{
                                            var sexList = new[]
                                            {
                                                new { Value = "", Text = "Chọn giới tính" },
                                                new { Value = "Nam", Text = "Nam" },
                                                new { Value = "Nữ", Text = "Nữ" }
                                            };
                                        }
                                        @Html.DropDownListFor(
                                            m => m.Sex,
                                            new SelectList(sexList, "Value", "Text"),
                                            new { @class = "auth-input auth-select" })
                                        @Html.ValidationMessageFor(m => m.Sex, "", new { @class = "text-danger small d-block mt-2" })
                                    </div>

                                    <div class="auth-form-group auth-grid-full">
                                        <label class="auth-label">Địa chỉ</label>
                                        @Html.TextBoxFor(m => m.Address, new
                                        {
                                            @class = "auth-input",
                                            placeholder = "Nhập địa chỉ của bạn"
                                        })
                                        @Html.ValidationMessageFor(m => m.Address, "", new { @class = "text-danger small d-block mt-2" })
                                    </div>
                                </div>

                                <div class="auth-form-row auth-form-row--register">
                                    <button type="submit" class="optical-btn optical-btn--dark auth-submit-btn" id="btnRegister">
                                        Đăng ký tài khoản
                                    </button>
                                </div>

                                <div class="auth-bottom-note">
                                    Đã có tài khoản?
                                    <a href="@Url.Action("LoginAccount","Account")">Đăng nhập ngay</a>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</section>

<div class="modal fade" id="customModal" tabindex="-1" role="dialog" aria-hidden="true">
    <div class="modal-dialog custom-modal-dialog modal-dialog-centered">
        <div class="modal-content auth-modal-content">
            <div class="modal-header auth-modal-header">
                <h5 class="modal-title">Hỗ trợ quên mật khẩu</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>

            <div class="modal-body auth-modal-body">
                <p>
                    Bạn hãy liên hệ qua thông tin bên dưới để được hỗ trợ cấp lại mật khẩu:
                </p>

                <div class="auth-support-list">
                    <a href="tel:0942962762">
                        <i class="fa fa-phone mr-2"></i>094.296.2762
                    </a>
                    <a href="mailto:nguyennhat025.go@gmail.com">
                        <i class="fa fa-envelope mr-2"></i>nguyennhat025.go@gmail.com
                    </a>
                </div>
            </div>

            <div class="modal-footer auth-modal-footer">
                <button type="button" class="btn btn-secondary" data-dismiss="modal">Đóng</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(function () {
            $("#registerForm").on("submit", function (e) {
                e.preventDefault();

                var form = $(this);

                if (form.valid && !form.valid()) {
                    return;
                }

                $("#btnRegister").prop("disabled", true).text("Đang xử lý...");
                $("#ajaxMessage").empty();

                $.ajax({
                    url: form.attr("action"),
                    type: "POST",
                    data: form.serialize(),
                    success: function (resp) {
                        if (resp.success) {
                            window.location.href = resp.redirectUrl;
                        } else {
                            showErrors(resp);
                        }
                    },
                    error: function () {
                        $("#ajaxMessage").text("Có lỗi xảy ra, vui lòng thử lại!");
                    },
                    complete: function () {
                        $("#btnRegister").prop("disabled", false).text("Đăng ký tài khoản");
                    }
                });
            });

            function clearFieldErrors() {
                $("span[data-valmsg-for]").text("");
            }

            function showErrors(resp) {
                clearFieldErrors();
                $("#ajaxMessage").empty();

                if (resp.globalErrors && resp.globalErrors.length > 0) {
                    $("#ajaxMessage").html(resp.globalErrors.join("<br/>"));
                }

                if (resp.fieldErrors) {
                    for (var key in resp.fieldErrors) {
                        if (!resp.fieldErrors.hasOwnProperty(key)) continue;

                        var msg = resp.fieldErrors[key];
                        var span = $("span[data-valmsg-for='" + key + "']");
                        if (span.length) {
                            span.text(msg);
                        }
                    }
                }
            }
        });
    </script>
}